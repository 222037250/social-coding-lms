// Social Coding LMS — frontend configuration
//
// Leave EMPTY when the Express server also serves these pages (Render, or
// `node server.js` locally). This is the normal setup.
//
// Set it ONLY if the frontend is hosted separately from the API — for example
// static files on Netlify with the server on Render:
//     window.SC_API_BASE = 'https://social-coding-lms.onrender.com';
window.SC_API_BASE = '';
